


    <div class="errinform">
       {{#if err}}
           This email is already used!
       {{/if}}
       {{#if emptyField}}
          Empty fields!
       {{/if}}
       {{#if thanks}}
          Thanks You for your time, I hope that you will came back!
       {{/if}}
    </div>

    <form method="POST">
            <div class="input">
                <input type='text' name='first' placeholder='First Name' autocomplete='off'>
            </div>
            <div class="input">
                <input type="text" name='last' placeholder='Last Name' autocomplete='off'>
            </div>
            <div class="input">
                <input type="text" name='email' placeholder='Email Adress' autocomplete='off'>
            </div>
            <div class="input">
                <input type="password" name='password' placeholder='Password' autocomplete='off'>
            </div>
            <input type="submit" value="Sing Up" class="submit">

        <div class="text">
            If You already have an account: <a href="/login">log in</a>
        </div>
        <input type="hidden" name="_csrf" value="{{csrfToken}}">
    </form>
